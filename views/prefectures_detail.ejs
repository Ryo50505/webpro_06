<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title><%= data.name %></title>
</head>
<body>
  <h2><%= data.name %>の詳細</h2>
  <table border="1">
    <tr><th>項目</th><th>データ</th></tr>
    
    <tr><td>ID</td><td><%= data.id %></td></tr>
    <tr><td>県名</td><td><%= data.name %></td></tr>
    <tr><td>県庁所在地</td><td><%= data.capital %></td></tr>
    <tr><td>人口</td><td><%= data.population.toLocaleString() %> 人</td></tr>
    <tr><td>名産</td><td><%= data.specialty %></td></tr>
    <tr><td>名所</td><td><%= data.spot %></td></tr>
    <tr><td>面積</td><td><%= data.area.toLocaleString() %> km²</td></tr>
    <tr>
      <td>画像</td>
      <td>
        <% if (data.image) { %>
          <img src="/public/<%= data.image %>" alt="<%= data.name %>" width="200">
        <% } else { %>
          画像なし
        <% } %>
      </td>
    </tr>
  </table>
  <br>
  <a href="/prefectures/edit/<%= data.id %>">編集</a>
  &nbsp;
  <a href="/prefectures/delete/<%= data.id %>" onclick="return confirm('削除しますか？');">削除</a>
  <br><br>
  <a href="/prefectures">一覧に戻る</a>
</body>
</html>